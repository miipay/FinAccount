import { Account } from './account.entity';
import { DepositWithdrawLedger, TransferLedger } from './ledger.entity';

const Entities = [Account, DepositWithdrawLedger, TransferLedger];

export { Entities, Account, DepositWithdrawLedger, TransferLedger };
